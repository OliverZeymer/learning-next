import styles from "../../styles";
const FaqPage = () => {
  return (
    <div className={`${styles.innerWidth} h-screen w-screen text-center ${styles.paddings}`}>
      <h1 className="text-6xl font-bold">FAQ Page</h1>
    </div>
  );
};
export default FaqPage;
